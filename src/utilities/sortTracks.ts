import { getAudioFeatures, getTrack } from "../fixtures/data";
import { Track, TrackID } from "../types";
import { useCache } from "./useCache";

export function sortTracks(comboTracks, sortParameter): TrackID[] {
    let sortedTracks: TrackID[] = [];

    return sortedTracks;
}